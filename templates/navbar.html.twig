<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" >Blog pas terrible</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_index') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('article_index') }}"> Articles </a>
            </li>


            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_list') }}">Categories</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('tag_list') }}" tabindex="-1" aria-disabled="true">Tags</a>
                </li>
            {% endif %}

            <li>
                {% if app.user.username is defined %}

                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}">logout</a>
                </li>


                {% else %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    </li>
                {% endif %}
            </li>

        </ul>
    </div>
</nav>

